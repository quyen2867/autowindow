# BROWSERS

This directory includes all the browser installers.

Currently, support:
- **Chrome**

## GOOGLE CHROME

This is an offline chrome installer. It installs a fresh `Chrome` browser with `default configuration`.